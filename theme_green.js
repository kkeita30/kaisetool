// 解説ボードのデザイントークン — グリーン系テーマ

window.THEME = {
  // ── ボードサイズ ──
  boardW: 1920,
  boardH: 1080,
  panelLeft:    20,
  panelTop:     20,
  panelWidth:   920,
  panelPadding: '24px 36px',

  // ── チャプタータイトル ──
  titleFont:    'MgenHeavy',
  titleSize:    50,
  titleColor:   '#e0ffe8',

  // ── ▼ 見出し ──
  midashiPrefix: '▼',
  midashiFont:  'MgenHeavy',
  midashiSize:  50,
  midashiColor: '#4ade80',   // 緑

  // ── ⇒ ポイント ──
  pointPrefix:  '⇒',
  pointFont:    'MgenBold',
  pointSize:    50,
  pointColor:   '#ffffff',
  pointStroke:  '2px #166534', // 深緑の縁取り

  // ── 　字下げ ──
  indentPadding: 20,
  indentFont:   'MgenLight',
  indentSize:   40,
  indentColor:  '#d1fae5',   // 薄緑

  // ── 平文 ──
  plainFont:    'MgenLight',
  plainSize:    40,
  plainColor:   '#d1fae5',

  // ── 行間 ──
  lineGap:      6,

  // ── 行頭記号とテキストの間隔 ──
  prefixGap:    8,
};
